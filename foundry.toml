[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc_version = "0.8.24"
optimizer = true
optimizer_runs = 200
# Verification: pass --metadata / --use-literal-content on forge verify if needed.

# Monad Testnet — from https://docs.monad.xyz/developer-essentials/testnets (fetched 2026-07-18)
eth-rpc-url = "https://testnet-rpc.monad.xyz"
chain_id = 10143

[rpc_endpoints]
monad_testnet = "https://testnet-rpc.monad.xyz"
